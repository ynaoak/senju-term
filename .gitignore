target/
node_modules/
dist/
*.log
.DS_Store
scratch-shots/

# Regenerated by tauri-build on every build, and per-platform (each OS emits
# its own <platform>-schema.json), so checking them in only produces churn and
# files that differ depending on who built last.
apps/desktop-app/src-tauri/gen/schemas/
__pycache__/

# Wrangler's local state (build cache, dev server data).
.wrangler/
