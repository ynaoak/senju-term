<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX manifest template for the Microsoft Store submission.

  This is a *template*: scripts/build-msix.ps1 substitutes the __…__ tokens and
  writes the result into the package layout. The identity values are not
  invented — Partner Center assigns them when the app is reserved, and the
  package is rejected unless Name/Publisher match the reservation byte for byte.
  See docs/release-and-distribution.md §5.

  The app runs as a full-trust desktop app (Windows.FullTrustApplication +
  runFullTrust): it spawns shells and opens SSH connections, which the
  restricted app container cannot do.
-->
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap rescap">

  <Identity
    Name="__IDENTITY_NAME__"
    Publisher="__PUBLISHER__"
    Version="__VERSION__"
    ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>Senju Term</DisplayName>
    <PublisherDisplayName>__PUBLISHER_DISPLAY_NAME__</PublisherDisplayName>
    <Logo>assets\StoreLogo.png</Logo>
    <Description>Warp-inspired terminal with built-in workflow and SSH connection management.</Description>
  </Properties>

  <Dependencies>
    <!-- 17763 (1809) is the floor for reliably shipping full-trust MSIX to the
         Store; WebView2 is present on Windows 11 and delivered by the Evergreen
         runtime on Windows 10. -->
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Resources>
    <Resource Language="ja-JP" />
    <Resource Language="en-US" />
  </Resources>

  <Applications>
    <Application Id="SenjuTerm" Executable="senju-term.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="Senju Term"
        Description="Warp-inspired terminal with built-in workflow and SSH connection management."
        BackgroundColor="#0D1117"
        Square150x150Logo="assets\Square150x150Logo.png"
        Square44x44Logo="assets\Square44x44Logo.png" />
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
